apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: referrals-app-ingress-v2
  namespace: referrals-ns-v2
  annotations:
    kubernetes.io/ingress.class: "gce"
    ingress.gcp.kubernetes.io/healthcheck-path: "/referrals/status"
spec:
  defaultBackend:
    service:
      name: referrals-app-svc-v2
      port:
        number: 8081