apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: referrals-app-ingress
  namespace: referrals-ns
  annotations:
    kubernetes.io/ingress.class: "gce"
    ingress.gcp.kubernetes.io/healthcheck-path: "/referrals/status"
spec:
  defaultBackend:
    service:
      name: referrals-app-svc
      port:
        number: 8081